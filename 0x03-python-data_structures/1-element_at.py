#!/usr/bin/python3

# CREATED BY OSEI YEBOAH ISAAC
def element_at(my_list, idx):
    if idx < 0:
        return (NULL)
    elif idx > len(my_list) - 1:
        return
    else:
        return(my_list[idx])
